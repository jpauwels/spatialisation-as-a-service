# Example env file — safe to commit. Copy to `.env.local` and fill values.
REACT_APP_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
REACT_APP_SECRET_ACCESS_KEY=YOUR_SECRET_ACCESS_KEY
REACT_APP_UPLOAD_BUCKET=saas-deposit
REACT_APP_OUTPUT_BUCKET=saas-output

# Do NOT commit real secrets into the repo. Use this as a template.